#pragma once
#include <iostream>
#include <fstream>
#include <vector>

using std::fstream; using std::cout; using std::cin; using std::vector;
//Titanic files to be general operative functions??
//include iterator in these files?
class Get {
    public:
        vector <int> getRow(vector <int> passenger);
        vector <int> passenger;
        vector <int> feature;

};
   
